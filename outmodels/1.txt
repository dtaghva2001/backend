import mongoose from "mongoose"; 
const someSchema = new mongoose.Schema({
SBUId:String 
})
export default const Student = mongoose.model(Student, someSchema);import mongoose from "mongoose"; 
const someSchema = new mongoose.Schema({
name:String 
SBUId:String 
})
export default const Student = mongoose.model(Student, someSchema);